#include <cstddef>
namespace resources
{
    namespace vector
    {
        constexpr size_t DEFAULT_DIMENSION = 3;
        constexpr double DEFAULT_VALUE = 0;
        constexpr double DEFAULT_COORDS[DEFAULT_DIMENSION]
            = {DEFAULT_VALUE, DEFAULT_VALUE, DEFAULT_VALUE};
    }
}